import React, {FC, useEffect} from 'react';

export const Vacancies: FC = () => {
    useEffect(() => {
        document.title = "Вакансии";
    }, [])
    return (
        <div>

        </div>
    );
};

