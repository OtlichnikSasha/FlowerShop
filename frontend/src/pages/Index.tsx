import React, {useEffect} from 'react';

export const Index: React.FC = () => {
    useEffect(() => {
        document.title = "Главная";
    }, [])

    return (
        <div>
            
        </div>
    );
};

